#!/bin/sh

# requires find and sed
require-cmd find set || exit 1

# generate theme-specific config files from templates
for i in $(find ${XDG_CONFIG_HOME:-$HOME/.config} ${XDG_DATA_HOME:-$HOME/.local/share} -type f -name '__*__'); do
    echo $i
done
