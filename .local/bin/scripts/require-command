#!/bin/sh

for i in $@; do
    command -v $i >/dev/null 2>&1 || {
        printf "$i: command not found\n"
        #notify-send "not installed" "$i must be installed for this function."
        exit 1
    }
done
